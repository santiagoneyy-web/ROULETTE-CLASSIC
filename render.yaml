services:
  - type: web
    name: roulette-classic
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run stack
    envVars:
      - key: PORT
        value: "10000"
      - key: GEMINI_API_KEY
        fromSecret: gemini_api_key
    autoDeploy: true
    healthCheckPath: "/"
